node_modules/
dist/
.astro/
.DS_Store
.env
.env.*
!.env.example
.blog-trash/

# Drafts are private (never committed). The nested .gitignore inside drafts/ allows
# tracking the ignore file itself while ignoring all actual draft content and assets.
src/content/drafts/
!src/content/drafts/.gitignore
exports/
